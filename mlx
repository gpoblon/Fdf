// void ligne(int pos.x,int pos.y,int pos_next.x,int pos_next.y)// {// 	t_coord	*i;// 	int dx,dy,i,xinc,yinc,cumul,x,y;// 	i.x = pos.x;// 	i.y = pos.y;// 	dx = pos_next.x - pos.x;// 	dy = pos_next.y - pos.y;// 	xinc = ( dx > 0 ) ? 1 : -1;// 	yinc = ( dy > 0 ) ? 1 : -1;// 	dx = abs(dx);// 	dy = abs(dy);// 	mlx_pixel_put(mlx, win, i.x * TILE_S, i.y * TILE_S, 0x00FFFFFF);// 	if (dx > dy)// 	{// 	    cumul = dx / 2;// 	    for (i = 1 ; i <= dx ; i++)// 		{// 			i.x += xinc;// 			cumul += dy;// 			if (cumul >= dx)// 			{// 		    	cumul -= dx;// 	    		i.y += yinc;// 			}// 			mlx_pixel_put(mlx, win, i.x * TILE_S, i.y * TILE_S, 0x00FFFFFF);// 	 	}// 	}//     else// 	{//     	cumul = dy / 2;// 		for ( i = 1 ; i <= dy ; i++)// 		{//     		y += yinc;//     		cumul += dx;//     		if ( cumul >= dy)// 			{//         		cumul -= dy;//         		i.x += xinc;// 			}//     	mlx_pixel_put(mlx, win, i.x * TILE_S, i.y * TILE_S, 0x00FFFFFF);// 		}// 	}// }